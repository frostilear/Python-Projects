import random
Lisr=[]
with open("UTF-8Chars.txt","r") as file:
    FileRead=str(file.read())
    def Splitted(word):
        return list(word)
    splitfile=Splitted(FileRead)
for letter in splitfile:
    letter=random.seed
    Lisr.append(letter)